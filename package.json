{
  "name": "inferweb",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://myt588.github.io/inferweb/",
  "dependencies": {
    "gh-pages": "^2.0.1",
    "react": "^16.6.0",
    "react-dom": "^16.6.0",
    "react-scripts": "2.0.5"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy-aws": "yarn build && aws s3 sync build/ s3://inferweb --delete",
    "deploy-github": "yarn build && gh-pages -d build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
